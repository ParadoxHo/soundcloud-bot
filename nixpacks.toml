[phases.setup]
cmds = [
    "apt-get update",
    "apt-get install -y ffmpeg curl",
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Building...'"]

[start]
cmd = "python main.py"
